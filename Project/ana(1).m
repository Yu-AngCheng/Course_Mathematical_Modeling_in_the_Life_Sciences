a=cat(2,alldat{:});
groupSpk=cat(2,a(:).groupSpk);
groupSpk=permute(groupSpk,[2 3 1]);
group=cat(1,a(:).group);
contrastLeft=cat(2,a(:).contrast_left);
contrastRight=cat(2,a(:).contrast_right);
response=cat(2,a(:).response);
responseTime=cat(1,a(:).response_time);
troughtoPeak=cat(2,a(:).trough_to_peak);
contrastLeftPassive=cat(2,a(:).contrast_left_passive);
contrastRightPassive=cat(2,a(:).contrast_right_passive);
groupSpkPassive=cat(2,a(:).groupSpkPassive);
groupSpkPassive=permute(groupSpkPassive,[2 3 1]);
wheel=cat(2,a(:).wheel);
wheel=reshape(wheel,10050,250);
wheelPassive=cat(2,a(:).wheel_passive);
wheelPassive=reshape(wheelPassive,4370,250);
feedbackType=cat(2,a(:).feedback_type);
feedbackTime=cat(1,a(:).feedback_time);
